appId: com.mlpmembers.app
productName: MLP Members
directories:
  buildResources: build
files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{.env,.env.*,.npmrc,pnpm-lock.yaml}"
  - "!backend/src/**"
  - "!backend/node_modules/**" # excluímos o node_modules raiz do backend

asarUnpack:
  - resources/**

extraResources:
  - from: backend/dist
    to: backend/dist

  - from: backend/package.json
    to: backend/package.json

  - from: backend/scripts
    to: backend/scripts

  - from: backend/prisma/schema.prisma
    to: backend/prisma/schema.prisma

  - from: backend/prisma/migrations
    to: backend/prisma/migrations

  - from: backend/node_modules
    to: backend/node_modules
win:
  executableName: MLP Members
  icon: build/icon.ico
  target:
    - nsis
    - zip

nsis:
  oneClick: false
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  createStartMenuShortcut: true
  allowToChangeInstallationDirectory: true

npmRebuild: false

publish:
  provider: github
  owner: lukasalencar
  repo: mlp_members_front
  releaseType: release
